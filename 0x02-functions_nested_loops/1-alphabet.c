#include "main.h"
/**
 * main - A function that prints alphabet in lower case
 *
 * Return: Always 0.
 */
void print_alphabet(void);
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(n);
	}
	_putchar('\n');
	return (0);
}

